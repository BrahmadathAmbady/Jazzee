import SwiftUI

import SwiftUI

/*struct ContentView: View {
    var body: some View {
        NavigationView {
            TabView {
                HomeView() // Home now includes swipeable job cards
                    .tabItem {
                        Image(systemName: "house.fill")
                        Text("Home")
                    }

                ProfileView()
                    .tabItem {
                        Image(systemName: "person.fill")
                        Text("Profile")
                    }
            }
            .navigationTitle("Jazzee")
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
*/
